apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: sonarqube-maven-example
  name: sonarqube-maven-example
spec:
  replicas: 1
  selector:
    matchLabels:
      app: sonarqube-maven-example
  template:
    metadata:
      labels:
        app: sonarqube-maven-example
      containers:
        - image: quay.io/praveenrachamreddy/ci-pipeline
          imagePullPolicy: Always
          name: sonarqube-maven-example
          ports:
            - containerPort: 80
              protocol: TCP

---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: sonarqube-maven-example
  name: sonarqube-maven-example
spec:
  type: NodePort
  ports:
    - name: 80-tcp
      port: 80
      targetPort: 80
      protocol: TCP
  selector:
    app: sonarqube-maven-example

---
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  labels:
    app: sonarqube-maven-example
  name: sonarqube-maven-example
spec:
  port:
    targetPort: 80-tcp
  to:
    kind: Service
    name: sonarqube-maven-example
    weight: 100
          
